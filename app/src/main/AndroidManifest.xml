<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zifu.mendibile">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mendibile">
        <activity android:name=".Compra.DetalleCompra"></activity>
        <activity android:name=".Compra.ListaCompra" />
        <activity android:name=".Compra.AgregaCompraGeneral" />
        <activity android:name=".Proveedores.DetalleProveedores" />
        <activity android:name=".Proveedores.AgregaProveedor" />
        <activity android:name=".Proveedores.ListaProveedores" />
        <activity android:name=".ListaPlt.AgregaPlato" />
        <activity android:name=".ListaPlt.ListaPlatos" />
        <activity android:name=".ListaIng.ListaIngredientes" />
        <activity android:name=".ListaIng.AgregaIngrediente" />
        <activity android:name=".Principal" />
        <activity android:name=".DetallePlatos.DetallePlatos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.zifu.mendibile"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>