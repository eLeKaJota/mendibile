<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zifu.mendibile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mendibile">
        <activity android:name=".ListaPlt.AgregaPlato"></activity>
        <activity android:name=".ListaPlt.ListaPlatos" />
        <activity android:name=".ListaIng.ListaIngredientes"/>
        <activity android:name=".ListaIng.AgregaIngrediente" />
        <activity android:name=".Principal" />
        <activity android:name=".DetallePlatos.DetallePlatos"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>